# ==========================================
# 개발 환경 설정 (로컬 백엔드 실행 시)
# ==========================================
# 이 파일은 npm run dev 실행 시 자동으로 사용됩니다.
# 로컬에서 백엔드(port 8000)와 AI-RAG(port 8001)도 함께 실행할 때 사용합니다.

# 개발 서버 포트 (백엔드 CORS 설정과 일치해야 함)
VITE_PORT=3000

# FastAPI 백엔드 URL (Vite 프록시 사용)
VITE_FASTAPI_URL=/api

# Gemini AI Service (Vite 프록시 사용)
VITE_GEMINI_FASTAPI_URL=/gemini-api

# 이메일 로그인 활성화 (개발 환경)
VITE_ENABLE_EMAIL_LOGIN=true

# ==========================================
# 사용 방법
# ==========================================
# 1. 루트 디렉토리에서: ./start_servers_tmux.sh
# 2. 또는 개별 실행:
#    - Backend: cd backend/backend && poetry run uvicorn app.main:app --reload --port 8000
#    - AI-RAG: cd AI-RAG && python main.py
#    - Frontend: cd frontend && npm run dev
