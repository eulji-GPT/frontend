# ==========================================
# 로컬 개발 환경 설정 (프론트엔드만 실행 시)
# ==========================================
# 이 파일은 .env.development보다 우선순위가 높습니다.
# Git에 커밋되지 않으므로 개발자마다 다른 설정을 사용할 수 있습니다.
# 백엔드를 로컬에서 실행하지 않고 프로덕션 API에 직접 연결할 때 사용합니다.

# 개발 서버 포트 (백엔드 CORS 설정과 일치해야 함)
VITE_PORT=3000

# FastAPI 백엔드 URL - 프로덕션 API 직접 연결
VITE_FASTAPI_URL=https://fastapi-backend-production-2cd0.up.railway.app

# Gemini AI Service - 프로덕션 API 직접 연결
VITE_GEMINI_FASTAPI_URL=https://fastapi-backend-production-2cd0.up.railway.app

# 이메일 로그인 활성화 (개발 환경)
VITE_ENABLE_EMAIL_LOGIN=true

# ==========================================
# 사용 방법
# ==========================================
# 프론트엔드만 실행:
#   cd frontend && npm run dev
#
# 테스트 계정:
#   이메일: dev@test.com
#   비밀번호: dev12345
#
# 주의:
# - 이 파일은 Git에 커밋되지 않습니다 (.gitignore 설정됨)
# - 프로덕션 백엔드에 직접 연결하므로 실제 데이터가 사용됩니다
# - 로컬 백엔드를 실행하려면 이 파일을 삭제하거나 이름을 변경하세요
